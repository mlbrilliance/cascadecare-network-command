# Coding-agent screenshots — capture checklist

**Channel 4** of the evidence set. Images are a human capture step (a coding agent can't
screenshot its own terminal retroactively), so this channel is **scaffolded with an honest
pending status** rather than fabricated.

## To capture (submission session — Slice 017)

- [ ] Claude Code authoring a `caseplan.json` via the `uipath-maestro-case` skill.
- [ ] The offline `uip solution pack` probe that root-caused Error 2005 (terminal).
- [ ] A green `uv run pytest` run (472+ tests).
- [ ] `/audit-ip-safety` passing on a commit.
- [ ] The Studio Web canvas showing the deployed master case (R3 fan).

Drop PNGs here named `NN-description.png` and link them from
[`../../../CODING_AGENTS.md`](../../../CODING_AGENTS.md).

**Status: PENDING capture.** No images committed yet.
