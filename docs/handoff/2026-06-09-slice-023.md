# CascadeCare — Session Handoff (2026-06-09)

Hand-off for a fresh agent to continue **slice-023** seamlessly. Read this + run `make resume`,
then skim the referenced artifacts. Do **not** re-derive what's already captured below — follow
the pointers.

---

## 1. Where things stand (DONE — verified live)

The full **three-level Maestro Case walk** works end-to-end and is pushed:
`clearflow-master-crisis` → 6× `clearflow-stakeholder-parent` → `clearflow-obligation-grandchild`.

- Branch **`slice-023-autowalk-5stages`** pushed to `github.com/mlbrilliance/cascadecare-network-command` @ **`e24887e`**.
- Deployed solution **`clearflow-solution` 1.0.20** → folder **`Shared/CascadeCare-v110`** (active).
- The grandchild-spawn fix: re-pointed `tfmtATbvb` entry from "after BAA" → `current-stage-entered`
  (eliminated a stage-exit race). Commit `a74b0f5`.
- Verified live: child (started directly) spawned grandchild `daa72ae7`, which ran
  classify-obligation + generate-audit-record. Master HITL gate + Slack close-out already worked
  in prior runs.

**Full deep history is in auto-memory** — read it first:
`~/.claude/projects/-mnt-c-Users-linki-OneDrive-Desktop-cascade-command/memory/hitl-action-center-app-details.md`
(the v1.0.13→1.0.20 narrative, every fix + every gotcha, with the live-proof + corrections).

Key code artifacts (don't re-read unless editing):
- `scripts/merge-child-grandchild.py` — re-applies child+grandchild fixes after any canvas download.
- `scripts/merge-canvas-download.py` — same for the master case.
- `scripts/pack-solution.sh` — assembles the solution from canonical sources.
- `docs/DEMO-RUNBOOK.md` — run + redeploy + auth + troubleshooting (Paths A/B).
- `docs/DEFERRED-FIXES.md` — the open polish list (your next task is P1 there).

---

## 2. YOUR NEXT TASK — DEFERRED-FIXES **P1** (grandchild HITL app)

**Problem:** the grandchild "Prepare & File Obligation Response" gate auto-submits instead of
pausing. **Root cause is A/B-proven: it's the app, not the case.** Same gate paused when bound to
the master's `CascadeCare` app; auto-completed on its own app. The app
`Regulatory/Contractual Obligation Response` (id `de1f291b-ff90-47b7-87b3-d7a08db1792b`,
Autopilot-generated) has **no human-required outcome**.

**Steps (full detail in `docs/DEFERRED-FIXES.md` P1 + the memory file):**
1. The USER must edit the app in **Studio Web → Apps**: add a blocking action outcome (a
   submit/approve button that holds the task open), mirroring how `CascadeCare` was built.
   Republish into `Shared/CascadeCare-v110`. *(This is a human/UI step — you cannot do it via CLI;
   Apps are SPA-gated.)*
2. The canonical case already binds the gate to `de1f291b` (the CascadeCare binding live in 1.0.20
   was a **test stopgap**, reverted in the repo). After the app is fixed: redeploy from canonical
   (RUNBOOK Path B) → start a child → confirm grandchild gate `t9BUmAX8k` goes **InProgress**
   (paused) and renders an Action Center task.

**⚠️ Drift to resolve:** live 1.0.20 grandchild gate = `CascadeCare` (pauses, wrong content);
repo = `de1f291b` (correct app, auto-completes until P1 done). A clean Path-B redeploy now would
un-pause the gate — do P1 first. (Documented in DEFERRED-FIXES "TRACKING".)

---

## 3. Operational facts you'll need

| Thing | Value |
|---|---|
| Tenant | `staging.uipath.com` / org `hackathon26_042` / `DefaultTenant` |
| Folder key (`$FK`) | `de7b7c18-d743-4c8c-b555-9bd3b96fe524` |
| Studio Web solution id | `0fc02d30-9645-45b6-4ed8-08dec248fc52` |
| Master / child / grandchild process keys | `AC365BA5-…` / `98CE7F31-…` / `5C399FE2-…` (RUNBOOK has full) |
| Grandchild HITL app | `de1f291b-ff90-47b7-87b3-d7a08db1792b` |

**Auth (it WILL be expired in a new session):** the `uip` token lapses (~58 h). Re-login:
`uip login --authority https://staging.uipath.com --organization hackathon26_042 --interactive`
(plain `uip login` wrongly targets `cloud.uipath.com`). RUNBOOK A1 has the gotchas.

---

## 4. Gotchas that cost real time (don't relearn them)

- **Read-API flakiness:** `uip maestro case instance list/get` and `uip or jobs list` intermittently
  return empty/`null` on this tenant. Cross-check with `uip or jobs get <key>` (reliable). The
  surest level-3 proof is a `clearflow-obligation-grandchild` **job** existing.
- **Coded-agent cold-start:** the master's claim-flow / multi-customer agents can hang 10–15 min
  (env, not defect). To test level-2→3 fast, start a **child** directly — its stages use low-code
  agents + APIs only.
- **Required ≠ the lever:** toggling a task "Required" does NOT fix non-firing; entry-condition
  timing does. (Cost a full investigation — see memory "CORRECTION".)
- **Every solution upgrade** resets 6 low-code agent names to PascalCase → re-run the kebab rename
  (RUNBOOK B6). **BAA-clean** the zip before publish (RUNBOOK B3).
- **`.git/index.lock`** stale-locks on this OneDrive/WSL mount — `rm -f .git/index.lock` and retry.
- **Secret-scanner hook** blocks any command containing a PAT — pushes need the user to set the
  tokenized remote (or push themselves). A PAT was shared in the prior session — assume it's being
  rotated; do NOT reuse it.
- **IP-safety:** run `/audit-ip-safety` before any commit (zero real-company tokens). Prior audits
  PASS (only denylist-definition matches).

---

## 5. Suggested skills to invoke

- **`uipath-agents`** — for the `de1f291b` app fix context (Agent Builder / Apps patterns).
- **`uipath-maestro-case`** — if you touch caseplan/.bpmn (V20 rules; runtime executes the COMPILED
  `caseplan.json.bpmn`, regenerated only by the Studio Web canvas).
- **`audit-ip-safety`** — before every commit.
- `/improve-codebase-architecture` + `/thermo-nuclear-code-quality-review` — paired, on the slice
  diff, per project CLAUDE.md (if doing more code).

---

## 6. Resume command for the new session

```
make resume && cat docs/handoff/2026-06-09-slice-023.md
```

Then read the memory file in §1, and start at §2 (DEFERRED-FIXES P1).
