# Build Roadmap — CascadeCare Network Command

> **This file is a pointer.** The canonical, active slice plan now lives at
> [`specs/003-uipath-native/tasks.md`](specs/003-uipath-native/tasks.md).

The active build plan is the pure-UiPath slice plan at `specs/003-uipath-native/tasks.md`
(Slices 004 → 017). Every slice ends with a green verify gate and a commit. Consult that
file — not this one — for current task status, dependencies, and acceptance criteria.

The previous pre-pivot roadmap that lived here (a PostgreSQL / LangGraph / Next.js / FastAPI
runtime with its own slice numbering and spec-kit feature paths) was **superseded by the
Slice 004 pure-UiPath pivot** on 2026-05-26. The Maestro Case canvas is now the runtime; no
external Python service runs at demo time. The archived predecessor spec is under
[`docs/archive/`](docs/archive/). For the architectural rationale, see
[`specs/003-uipath-native/plan.md`](specs/003-uipath-native/plan.md) and the
[changelog](docs/changelog.md).
